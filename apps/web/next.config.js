/** @type {import('next').NextConfig} */
const nextConfig = {
    turbopack: {
        root: '/home/apoll011/coding/together', // ajusta para a raiz do monorepo onde next está instalado
    },
};

export default nextConfig;
